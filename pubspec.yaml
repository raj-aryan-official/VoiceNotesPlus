name: voice_notes_plus
description: "Voice Notes Plus - Record, transcribe, and manage your voice notes"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  speech_to_text: ^7.0.0
  record: ^5.1.2
  path_provider: ^2.1.2
  intl: ^0.19.0
  audioplayers: ^6.0.0
  permission_handler: ^11.3.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.12

flutter:
  uses-material-design: true
